// This file is for ESLint's new flat config format, but your project is set up with 'eslint-config-next',
// which is designed for the traditional .eslintrc.json format.
//
// To get ESLint working, please do the following:
// 1. Delete this file (`eslint.config.js`).
// 2. Create a new file named `.eslintrc.json` in the same directory (`src/client`).
// 3. Add the following content to `.eslintrc.json`:
// {
//   "extends": "next/core-web-vitals"
// }
//
// This will correctly apply Next.js's recommended ESLint rules to your project.
