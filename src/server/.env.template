# For env vars related to the Google project and Auth0 setup, comment in the discussion titled 'Request Environment Variables (.env) Here' (https://github.com/existence-master/Sentient/discussions/13) Make sure to keep the values in this file empty while committing to the repository

NEO4J_URI=
NEO4J_USERNAME=
NEO4J_PASSWORD=
GOOGLE_CLIENT_ID=
GOOGLE_PROJECT_ID=
GOOGLE_CLIENT_SECRET=
AES_SECRET_KEY= # 256 bits or 32 chars
AES_IV= # 256 bits or 32 chars
AUTH0_MANAGEMENT_CLIENT_ID=
AUTH0_MANAGEMENT_CLIENT_SECRET=
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_AUDIENCE=
AUTH0_SCOPE='openid profile email offline_access read:chat write:chat read:profile write:profile manage:google_auth read:memory write:memory read:tasks write:tasks read:notifications read:config write:config admin:user_metadata'
APP_SERVER_PORT=
MONGO_URI=mongodb://localhost:27017/ # Often includes the default database name in the URI itself, or it's specified separately.
MONGO_DB_NAME=sentient_agent_db
LLM_PROVIDER= # Options: "OLLAMA", "OPENROUTER"
STT_PROVIDER= # Options: "FASTER_WHISPER", "ELEVENLABS"
TTS_PROVIDER= # Options: "ORPHEUS", "ELEVENLABS", "GCP"
# TTS_PROVIDER can be "GCP" or "ELEVENLABS" when developer_mode is false
# TTS_PROVIDER=GCP
# ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# GitHub OAuth App Credentials
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# NewsAPI Key
NEWS_API_KEY=

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# --- Kafka Configuration ---
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
CONTEXT_EVENTS_TOPIC=gmail_polling_results,gcalendar_polling_results
MEMORY_OPERATIONS_TOPIC=memory_operations
ACTION_ITEMS_TOPIC=action_items
NOTION_MCP_SERVER_URL=http://localhost:9009/sse
GITHUB_MCP_SERVER_URL=http://localhost:9010/sse
NEWS_MCP_SERVER_URL=http://localhost:9012/sse
CHAT_TOOLS_MCP_SERVER_URL=http://localhost:9013/sse
GDOCS_MCP_SERVER_URL=http://localhost:9004/sse
GSLIDES_MCP_SERVER_URL=http://localhost:9014/sse
GSHEETS_MCP_SERVER_URL=http://localhost:9015/sse