# HTTP Server configuration for FastMCP
MCP_SERVER_HOST=127.0.0.1
MCP_SERVER_PORT=9026

# Temporary directory for user files
FILE_MANAGEMENT_TEMP_DIR=/tmp/sentient_files

# How often to clean old files, in seconds (e.g., 3600 = 1 hour)
FILE_CLEANUP_INTERVAL_SECONDS=3600

# How old a file must be to be deleted, in seconds (e.g., 86400 = 24 hours)
FILE_MAX_AGE_SECONDS=86400