# HTTP Server configuration for FastMCP
MCP_SERVER_HOST=127.0.0.1
MCP_SERVER_PORT=9027

# Temporary directory for user files (should match main server config)
FILE_MANAGEMENT_TEMP_DIR=/tmp/sentient_files
# Path to the system-wide LinkedIn cookies file.
LINKEDIN_COOKIES_PATH=/path/to/your/linkedin_cookies.json
